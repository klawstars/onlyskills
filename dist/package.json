{
  "name": "theme-212660-local-preview",
  "version": "1.0.0",
  "private": true,
  "description": "Local preview server for Nunjucks theme templates",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build.js",
    "build:theme": "node -e \"const fs=require('fs');if(!fs.existsSync('Theme/package.json')){console.error('Theme/package.json not found. Restore the Theme folder first.');process.exit(1)}\" && npm --prefix Theme run build",
    "dev:theme": "npm --prefix Theme run dev",
    "preview:theme": "npm --prefix Theme run preview"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "nunjucks": "^3.2.4",
    "sanitize-html": "^2.17.2"
  }
}
